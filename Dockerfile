# Uses Tensorflow 2.9.x on GPU and comes with jupyter
FROM tensorflow/tensorflow:latest-gpu-jupyter


# include commands to install additional software with pip

# copy some files in?

# entry point?
